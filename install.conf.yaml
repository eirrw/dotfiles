- defaults:
    link:
        relink: true
        
- clean: ['~']

- link:
    ~/.tumx.conf: tmux.conf
    ~/.vimrc: .vimrc
    ~/.zshrc: 

- shell:
    - [git submodule update --init --recursive, Installing submodules]


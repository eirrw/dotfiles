- defaults:
    link:
        relink: true
        
- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tumx.conf: tmux.conf
    ~/.vimrc: .vimrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]


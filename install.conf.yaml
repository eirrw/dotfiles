- defaults:
    link:
        relink: true
        
- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux: tmux/
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: 
    ~/.zshrc: 

- shell:
    - [git submodule update --init --recursive, Installing submodules]

